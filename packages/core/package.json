{
  "name": "@persona-counsel/core",
  "version": "0.0.0-placeholder",
  "description": "Placeholder scoped package for npm scope reservation",
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "scripts": {
    "prepublishOnly": "node -e \"if (!process.env.PERSONA_COUNSEL_RELEASE) { console.error('Direct npm publish is disabled; use \\\"npm run release\\\" from repo root.'); process.exit(1); }\""
  }
}
